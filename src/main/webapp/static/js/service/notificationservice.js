var zionNotifacationService = (function () {    toastr.options = {        "closeButton": false,        "debug": false,        "newestOnTop": true,        "progressBar": true,        "positionClass": "toast-top-full-width",        "preventDuplicates": true,        "onclick": null,        "showDuration": "300",        "hideDuration": "1000",        "timeOut": "5000",        "extendedTimeOut": "1000",        "showEasing": "swing",        "hideEasing": "linear",        "showMethod": "fadeIn",        "hideMethod": "fadeOut"    };    var info = function (content, title) {        var t = title;            if (!title) {                t = 'Info';            }            // toastr.warning(content, t);        },        success = function (content, title) {            var t = title;            if (!title) {                t = 'Success';            }            // toastr.success(content, t);        },        error = function (content, title) {            var t = title;            if (!title) {                t = 'Error';            }            // toastr.error(content, t);        };    return {        info: info,        success: success,        error: error    }})();